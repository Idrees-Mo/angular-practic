import { BacsicDrictiveDirective } from './bacsic-drictive.directive';

describe('BacsicDrictiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BacsicDrictiveDirective();
    expect(directive).toBeTruthy();
  });
});
